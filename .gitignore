
venv/
.env
chroma_db_terraform/
terraform-provider-aws/
__pycache__/
*.pyc
.DS_Store

# SQLite checkpointer state files
state.db
state.db-shm
state.db-wal
test.db
test_sync.db

# Logs & scratch
streamlit.log
scratch/
*.log

# Security scan outputs (generated in CI — never commit)
garak_runs/
*.garak
trivy-results.sarif
semgrep.sarif
promptfoo-results.json
promptfoo-report.html
promptfoo.sarif

gcp-ci-key.json
jobs.db
job_store.db
*.db
*.db-shm
*.db-wal

# Persistent Terraform workspaces (contain real state files — never commit)
workspaces/
check_db.py
check_jobs.py
